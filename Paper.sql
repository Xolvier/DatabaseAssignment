CREATE TABLE Paper
(
  Id       INT(150) NOT NULL,       
  Title            VARCHAR(150) NOT NULL,  
  Abstract          VARCHAR(150) NOT NULL, 
  FileName          VARCHAR(150) NOT NULL,     
  PRIMARY KEY     (Id)                 
);