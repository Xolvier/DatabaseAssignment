CREATE TABLE Paper
(
  Id       INT NOT NULL,       
  Title            VARCHAR (100),  
  Abstract          VARCHAR (1000), 
  FileName          VARCHAR (100),     
  PRIMARY KEY     (Id)                 
);